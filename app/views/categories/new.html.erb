<h1>Add a recipe to <%= @category.name %> category</h1>
<%= simple_form_for [@category, @bookmark] %>

<h1>Create a new Category</h1>

<%= simple_form_for @category do |f| %>
<%= f.input :name %>
<%= f.submit %>
<% end %>
